spring:
  application:
    name: fundamentos01
  datasource:

    url: jdbc:postgresql://${DB_HOST:ep-rough-art-aivb6r9h-pooler.c-4.us-east-1.aws.neon.tech}:${DB_PORT:5432}/${DB_NAME:neondb}?sslmode=require
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_dZk5ySXj7IHl}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  jwt:
    secret: ${JWT_SECRET:mySecretKeyForJWT2024MustBeAtLeast256BitsLongForHS256Algorithm}
  
    expiration-ms: 1800000 
    refresh-expiration: 604800000
    issuer: fundamentos01-api
    header: Authorization
    prefix: "Bearer "